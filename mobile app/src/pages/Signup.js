import { SafeAreaView, Text } from "react-native";

function Signup({navigation}){
    return(
        <SafeAreaView>
            <Text>Signup</Text>
        </SafeAreaView>
    )
}

export default Signup;